/**
 * CHALLENGE 2
 * 
 * Create a function "concatenateStrings" and it will have 2 parameters.
 * And this function will return cancatenated string.
 */

function concatenateStrings ( str1, str2 ) {
  return str1 + " " + str2;
}

const str3 = concatenateStrings( "Hello", "world!" );

console.log( str3 );