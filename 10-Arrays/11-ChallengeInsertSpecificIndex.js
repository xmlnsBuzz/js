/**
 * Add element at the specfic index.
 * Element "abc" with index 10.
 * Log to console Array and explain results.
 * What is the length of the fianl Array.
 */
const myArray = [ 1, 2 ];
console.log( myArray.length ); // 2

myArray[ 10 ] = "abc";
console.log( myArray ); // ()11) [1, 2, empty × 8, "abc"]
console.log( myArray.length ); // 11